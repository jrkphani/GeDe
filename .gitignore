# Dependencies / build
node_modules/
dist/
dev-dist/
coverage/

# Test artifacts
test-results/
playwright-report/

# Env & local config
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.local

# Local Claude Code state + transient agent worktrees
.claude/

# Source design documents (private working material — not published)
*.numbers
*.pages
*.pdf

# Generated knowledge graph
graphify-out/

# Local throwaway working dir (e2e artifacts, probe scripts, HAR dumps)
scratchpad/

# CDK deploy app (issue 040) — self-contained, own deps/build output.
# (node_modules/ and dist/ above already match these, listed explicitly
# per the deploy app's own gitignore contract.)
deploy/cdk/node_modules/
deploy/cdk/cdk.out/

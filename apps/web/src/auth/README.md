# Auth — which Amplify APIs, and why

Cognito user pool in choice-based (`USER_AUTH`) mode: passkeys first, e-mail one-time codes as fallback, Sign in with Apple as an OIDC federated provider. No password is ever collected, stored or sent.

All calls go through `src/auth/cognito.ts`; screens never import `aws-amplify` directly. Tests mock that module only.

SDK verified: `aws-amplify@6.20.0` / `@aws-amplify/auth@6.20.0`.

| Need                                                | API used (from `aws-amplify/auth` unless noted)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Type-def verified                                                                                                                                                                                                                                                                      |
| --------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Configure                                           | `Amplify.configure({ Auth: { Cognito: { userPoolId, userPoolClientId, loginWith: { email: true, oauth? } } } })` then `cognitoUserPoolsTokenProvider.setKeyValueStorage(sharedInMemoryStorage)` (`aws-amplify/auth/cognito`, `aws-amplify/utils`)                                                                                                                                                                                                                                                                                                                                                                            | `node_modules/aws-amplify/dist/esm/initSingleton.mjs` shows `configure` installs `defaultStorage` (localStorage) itself, so the memory store must be set _after_ it. `KeyValueStorageInterface` and `sharedInMemoryStorage` are exported from `@aws-amplify/core/dist/esm/index.d.ts`. |
| Passkey sign-in (AUTH-04/05)                        | `signIn({ username: email, options: { authFlowType: 'USER_AUTH', preferredChallenge: 'WEB_AUTHN' } })` — Amplify performs the `navigator.credentials.get` ceremony itself and returns `nextStep.signInStep === 'DONE'`. Cancel surfaces as `PasskeyError` named `PasskeyAuthenticationCanceled` / `PasskeyOperationAborted`.                                                                                                                                                                                                                                                                                                 | `@aws-amplify/auth/dist/esm/providers/cognito/types/options.d.ts` (`SignInOptions.authFlowType`, `preferredChallenge: AuthFactorType`), `.../types/models.d.ts` (`AuthFactorType = 'WEB_AUTHN' \| 'EMAIL_OTP' \| …`), `.../client/utils/passkey/errors.d.ts`                           |
| Code sign-in (AUTH-04/06)                           | `signIn({ …, preferredChallenge: 'EMAIL_OTP' })` → `nextStep.signInStep === 'CONFIRM_SIGN_IN_WITH_EMAIL_CODE'` → `confirmSignIn({ challengeResponse: code })`. Resend = call `signIn` again (a new session, a new code).                                                                                                                                                                                                                                                                                                                                                                                                     | `@aws-amplify/auth/dist/esm/types/models.d.ts` lines 166–191                                                                                                                                                                                                                           |
| Passwordless sign-up (AUTH-03)                      | `signUp({ username: email, options: { userAttributes: { email, name }, autoSignIn: { authFlowType: 'USER_AUTH', preferredChallenge: 'EMAIL_OTP' } } })` — **no `password`**. Then `confirmSignUp({ username, confirmationCode })` → `nextStep.signUpStep === 'COMPLETE_AUTO_SIGN_IN'` → `autoSignIn()`. Amplify stores the `Session` returned by SignUp/ConfirmSignUp and forwards it into `InitiateAuth(USER_AUTH)`, so the address verification doubles as the first factor and no second code is needed. If auto sign-in did not start, we fall back to a normal code sign-in. Resend = `resendSignUpCode({ username })`. | `AuthSignUpInput.password?: string` is optional — `@aws-amplify/auth/dist/esm/types/inputs.d.ts` line 102–106; `signUp.mjs` only sets `Password` when present; `signUpHelpers.mjs`/`signInWithUserAuth.mjs` (`handleUserAuthFlowInput.session = autoSignInStoreState.session`).        |
| Locale attribute (I18N-05)                          | `signUp` carries `userAttributes.locale` (the device's active locale) and `updateUserAttributes({ userAttributes: { locale } })` mirrors a later change; the pool's custom-message trigger reads that attribute to render every one-time code in the account's language (`infra/assets/custom-message`, ADR-044). The session backfills it once when the account's server-side locale differs. The SPA client may write `locale` (infra `writeAttributes`).                                                                                                                                                                  | `@aws-amplify/auth/dist/esm/providers/cognito/apis/updateUserAttributes.d.ts`; `AuthUserAttributes` keys include `locale` (`types/models.d.ts`)                                                                                                                                        |
| Sign in with Apple (AUTH-08)                        | `signInWithRedirect({ provider: 'Apple' })`; needs `loginWith.oauth.domain` — hence `appleSignIn: { domain }` in `config.json`. Shown only when configured.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `.../providers/cognito/apis/signInWithRedirect.d.ts`, `AuthProvider` in `types/inputs.d.ts`                                                                                                                                                                                            |
| Passkey registration after a code sign-in (AUTH-07) | `associateWebAuthnCredential()`; a decline is stored in `localStorage['gede.passkeyOfferDeclinedAt']` and not re-offered for 30 days.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `@aws-amplify/auth/dist/esm/index.d.ts` line 6                                                                                                                                                                                                                                         |
| Session (AUTH-09)                                   | `fetchAuthSession()` for the bearer token (silent refresh inside Amplify); `getCurrentUser()` + `fetchUserAttributes()` for the header; `Hub.listen('auth', …)` for `signedIn` / `signedOut` / `tokenRefresh_failure`.                                                                                                                                                                                                                                                                                                                                                                                                       | `@aws-amplify/core/dist/esm/Hub/types/AuthTypes.d.ts`                                                                                                                                                                                                                                  |
| Sign out (AUTH-09)                                  | `signOut({ global: false })` — revokes the refresh token and clears the (memory) token store.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `AuthSignOutInput` in `types/inputs.d.ts` line 29                                                                                                                                                                                                                                      |

## Things the pool must have (infra)

- Sign-in options `USER_AUTH` with `EMAIL_OTP` and `WEB_AUTHN` enabled; passkey relying-party id = the web origin host.
- `email` as the sign-in alias; `name` as a mutable standard attribute; self sign-up allowed; verification by code (not link).
- Apple registered as an OIDC provider on the pool and the hosted-UI domain exposed as `appleSignIn.domain`.
- `PreventUserExistenceErrors` is **on** in production, so an unknown email never raises `UserNotFoundException`. Cognito answers `InitiateAuth` with `SELECT_CHALLENGE` and the pool's generic factors (`PASSWORD_SRP`, `PASSWORD`, `WEB_AUTHN`) — never `EMAIL_OTP`, which a known account with a verified address always lists. `toStep` maps that shape to `AuthFailureError({ kind: 'unknown-email' })` so the method step reads "No account uses this email. Switch to Create account to start one." (AUTH-04, #46). `classifyError` still maps `UserNotFoundException` for a pool with the flag off.

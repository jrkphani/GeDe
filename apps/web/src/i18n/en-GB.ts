import { messages as enUS } from './en-US.js';
import type { Messages } from './messages.js';

/** English (United Kingdom): the tour copy has no spelling that differs from en-US. */
export const messages: Messages = { ...enUS };

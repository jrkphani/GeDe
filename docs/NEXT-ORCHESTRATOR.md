# NEXT ORCHESTRATOR — launch prompt: 089 D3 graduation (P7 default-flip + re-triage), fresh session

> Copy the block below as the launch prompt for the next orchestrator agent. It drives the **089 canvas-graduation build to completion** — P0–P6 + 093 are SHIPPED + DEPLOYED, so **only P7 remains** — then re-triages the backlog to **open-issue count 0**. **P7 is the LAST phase and the ONLY reversing, prod-behavior-changing step** (it makes the canvas the default UI). Go carefully; MANDATORY adversarial review. Lean on subagents (≤3 concurrent). The throwaway password is intentionally NOT embedded — pass it inline at launch (`GEDE_PASSWORD=…`); never commit it.

---

You are the ORCHESTRATOR for the GeDe repo (`/Users/jrkphani/Projects/GeDe`). Finish the **089 D3-canvas GRADUATION build** — only phase **P7** remains (P0/P1/P2/093/P3/P4/P5/P6 are SHIPPED + LIVE) — then re-triage `docs/issues/README.md` until the open-issue count is **0**. You may `git push`, merge, and deploy (push to `main` → CI `verify` → `deploy` via `workflow_run` on verify-success), and run live-smokes against production with the throwaway creds passed at launch (`GEDE_PASSWORD=<from owner>`, `GEDE_EMAIL='jrkphani@gmail.com'`; rotate after; never commit).

**START by reading, in order:** `docs/HANDOFF.md` (START HERE — current state, the established PATTERNS, the full banked P7 map with verified file:line targets, and the non-negotiables); then `docs/issues/089-unified-canvas-workspace.md → ## D3 GRADUATION — APPROVED BUILD PLAN` (the authoritative phased spec — **P0–P6 + 093 are marked SHIPPED with lessons; you start at P7**), especially the P7 bullet (`:275-278`) and the `### SITEMAP + 085 lockstep edits` subsection (`:287-293`).

## What's already shipped (do NOT redo)
P0 `74ae9a2` (flag persistence, canvas-mode store) · P1 `d2f7fe7` (Foundation per-item nodes, reorders by **rank**) · P2 `e0cc006` (Design `register`+`ring`, `canvasCompose.ts`) · 093 `dd8902b` (register extend-right + LOD) · P3 `da87e86` (recursion satellites — FIRST RF edges + `clusterLayout.ts` + `canvasSatellites.ts`; drill → summary-STUB satellite) · P4 `4917219` (fully-live coverage TWIN — `canvasCoverage.ts`; `v` opens a `coverageTwin` node; `routes.ts` untouched) · P5 `4ee3033` (lane LOD summary cards, edit-safe focus-ref `useLaneLod` + register width-cap + volume e2e) · **P6 `e4a66b2`+`e81ab4e` (deploy-gate transition — the 21 canvas specs now RUN IN the gating `verify`; `dev-canvas-e2e.yml` retired; the focus-pan fixme became a pure unit test `workspaceFocusPan.ts`; `@dev-flag` tags RETAINED as a one-line rollback lever).** Live HEAD `e81ab4e`, CI-green. The flag-off `WorkspaceSurface`/`DesignSurface` are UNTOUCHED. **Tracked follow-up: promote the P3 satellite STUB → a live child {register+ring} core (needs a per-canvas store factory — the singleton `contexts` store blocks two live cores).**

## The remaining phase — P7 (all funnels through `src/App.tsx` + `src/store/canvasMode.ts`; ≤5 files/edit-set, red-first, with a Gate)

**P7 — THE DEFAULT-FLIP (last, the reversing step) + SITEMAP §1–4 / 085 supersession in the SAME PR. HIGHEST prod risk — MANDATORY adversarial review of the flip AND all doc edits.** The full verified map is in `HANDOFF.md`; the essentials:
- **The gate is STORE-based — the doc's `d3CanvasEnabled()` is STALE (no such function).** Real edits: `src/store/canvasMode.ts:44` (+`:26-28,:45-46,:49`) — replace `d3rfInUrl()` with a new `canvasCapable()` helper (width ≥ 1024 + not `prefers-reduced-data`) and drop the `import.meta.env.DEV &&` prefix so the canvas ships by capability; `src/App.tsx:83-89` — invert the ternary so the canvas is primary and `WorkspaceSurface` is the `< 1024px` fallback. **Build the capability helper FROM SCRATCH** (clone the matchMedia pattern in `src/shell/laneTarget.ts:42-43`; align to the existing `base.css:341` 1024 boundary).
- **RF now ships → invert the bundle guard into a budget CEILING:** `vite.config.ts:30` — remove the `'**/WorkspaceCanvas-*'` `globIgnores` entry; rewrite `d3CanvasNav.guard.test.ts:6-20` intent comments (keyed on stale `import.meta.env.DEV`) to a size-budget assertion. **PROD-FOLD inverts here.**
- **e2e blast radius (verified):** `e2e/workspace.spec.ts` (`:21-30`, hardcoded `.workspace__lane--*`) breaks HARD — rewrite for `.wc-node--*` or re-pin as the `< 1024px` narrow smoke. ~23 files / ~57 non-`@dev-flag` blocks are at native-scroll-vs-transformed-viewport VISIBILITY risk — **run the full suite against the canvas in a branch FIRST to enumerate breakage** (D2 P5 Rule-12 sweep is the template).
- **SITEMAP/085 land in the SAME PR** (precise edits: 089 `:287-293`). **⚠ SITEMAP self-conflict:** `:287-293` (RETAIN route grammar) vs older `:315-325` (REMOVE routes) — the APPROVED-BUILD-PLAN subsection `:287-293` GOVERNS.
- **Gate:** full `verify` (all e2e, canvas-default) green; SITEMAP + 085 merged in the SAME PR; a narrow-viewport smoke still renders `WorkspaceSurface`; bundle within the new ceiling; screenshot the (now user-facing) canvas + a live-smoke of a real write.

## Workflow (per phase — divide-and-conquer with subagents)
**INVESTIGATE** (read-only `Explore` → verbatim file:line map, keeps it out of your context — much of P7 is already mapped in HANDOFF) → **RED-FIRST** (a failing unit/e2e for the Gate) → **IMPLEMENT** (a `fork` inherits your context for a large multi-file build; else inline) → **ADVERSARIALLY REVIEW** (`code-reviewer` on the diff — MANDATORY for P7) → **VERIFY yourself** (`npm run verify:fast` + the FULL `npm run e2e` (now includes canvas specs) + test EVERY route on the canvas + screenshot) → **COMMIT** (`--no-verify` after verifying + explicit `git add`) → push (`--no-verify` if a local e2e loop is running) → confirm CI `verify` green + `deploy` ran.

## Non-negotiables (full list in the HANDOFF)
- **Deploy = push to `main`;** watch CI with `gh run list --json` poll loops (`gh run watch` is flaky). **Rollback if a canvas spec flakes: re-add `--grep-invert @dev-flag` to `package.json` `e2e`.**
- **STALE-VITE:** `pkill -f "@playwright/test/cli.js test-server"; pkill -f vite; lsof -ti:5173 | xargs -r kill -9` before every e2e re-run.
- **PROD-FOLD inverts at P7** (RF ships → budget ceiling). Until then, grep `xyflow` stays OUT of the main bundle.
- **TEST EVERY route on the canvas** + navigate; watch for "Maximum update depth" tier-route bugs. **Keep `routes.ts` grammar intact** (P7 RETAINS it as deep-links).
- **≤3 concurrent subagents;** never let two edit the same file on `main`; worktree-isolate overlapping ones.
- **Adversarially review** MANDATORY for P7's default-flip + the SITEMAP/085 edits. **Screenshot** every user-facing change (canvas is user-facing from P7). **Schema only via migrations** (089 is frontend-only). CloudWatch (`…WriteApiFunction…`, profile `phani-quadnomics`, read-only) is the authoritative write-path live check.
- All 089/093 owner-forks are answered — **build autonomously**; surface a genuinely NEW fork via `AskUserQuestion`.

## Definition of done
089 graduated (canvas is default; the 011/012 cluster built; P7 shipped + SITEMAP/085 updated) + live-verified + archived to `docs/issues/done/`; the P3 stub→live-core follow-up shipped or filed as its own issue; then re-triage `docs/issues/README.md` until open-issue count is **0**. Leave a compressed HANDOFF update. **Loop until 0.**

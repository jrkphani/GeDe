# NEXT ORCHESTRATOR — launch prompt: 089 D3 graduation (P6→P7 + re-triage), fresh session

> Copy the block below as the launch prompt for the next orchestrator agent. It drives the **089 canvas-graduation build to completion** — P0/P1/P2/093/P3/P4/P5 are SHIPPED + DEPLOYED, so start at **P6** — then re-triages the backlog to **open-issue count 0**. P6 (arm the deploy gate) and P7 (reverse the prod default) are the two highest prod-risk phases; go carefully. Lean on subagents (≤3 concurrent). The throwaway password is intentionally NOT embedded — pass it inline at launch (`GEDE_PASSWORD=…`); never commit it.

---

You are the ORCHESTRATOR for the GeDe repo (`/Users/jrkphani/Projects/GeDe`). Finish the **089 D3-canvas GRADUATION build** — phases **P6→P7** (P0/P1/P2/093/P3/P4/P5 are SHIPPED + LIVE behind `?d3rf`) — then re-triage `docs/issues/README.md` until the open-issue count is **0**. You may `git push`, merge, and deploy (push to `main` → CI `verify` → `deploy` via `workflow_run` on verify-success), and run live-smokes against production with the throwaway creds passed at launch (`GEDE_PASSWORD=<from owner>`, `GEDE_EMAIL='jrkphani@gmail.com'`; rotate after; never commit).

**START by reading, in order:** `docs/HANDOFF.md` (START HERE — current state, the established PATTERNS, the banked P6 map, and the non-negotiables); then `docs/issues/089-unified-canvas-workspace.md → ## D3 GRADUATION — APPROVED BUILD PLAN` (the authoritative phased spec — **P0–P5 + 093 are marked SHIPPED with lessons; you start at P6**). Do not touch a phase before reading its spec bullet + the file:line it cites.

## What's already shipped (do NOT redo)
P0 `74ae9a2` (flag persistence) · P1 `d2f7fe7` (Foundation per-item nodes, reorders by **rank**) · P2 `e0cc006` (Design `register`+`ring`, `canvasCompose.ts`) · 093 `dd8902b` (register extend-right + LOD) · **P3 `da87e86`** (recursion satellites — FIRST RF edges + `src/domain/clusterLayout.ts` + `src/store/canvasSatellites.ts`; drill → summary-STUB satellite + edge) · **P4 `4917219`** (fully-live coverage TWIN — `src/store/canvasCoverage.ts`; `v`/toggle open a `coverageTwin` node; `routes.ts` untouched) · **P5 `4ee3033`** (lane LOD summary cards, edit-safe via a focus-ref `useLaneLod` + register width-cap + volume e2e). Live HEAD `2cd308f`, CI-green. The flag-off `WorkspaceSurface`/`DesignSurface` are UNTOUCHED (mutually-exclusive `?d3rf` path). **Tracked follow-up in the 089 issue: promote the P3 satellite STUB → a live child {register+ring} core (needs a per-canvas store factory — the singleton `contexts` store blocks two live cores).**

## Remaining phases (SEQUENTIAL — all funnel through `src/components/WorkspaceCanvas.tsx`; never edit it in two concurrent phases; each ≤5 files, red-first, with a Gate)

- **P6 — deploy-gate transition (the careful one — do NOT freeze deploys).** FIRST de-flake the ONE remaining `test.fixme` (focus-pan, `e2e/d3-canvas.spec.ts:375`) — its in-file note (`:355-374`) proves it can't be fixed black-box, so make `WorkspaceCanvas.onFocusCapture` await the measurement fit / expose a settled signal, **or** convert it to a UNIT test of the pan logic (`WorkspaceCanvas.tsx:1169-1192`) and delete the e2e. THEN prove the full **22 `@dev-flag`** spec suite green across **N (20–50) consecutive** `npm run e2e:dev-flag` runs while STILL `@dev-flag` (harden the 20 animated-motion specs first — consider `reducedMotion:'reduce'` project-wide, re-verifying the flag-off suite). ONLY THEN un-tag: drop `, { tag: '@dev-flag' }` from all 22 tests (`:375` `test.fixme(`→`test(`) + `--grep-invert @dev-flag` (`package.json:14`) + retire `.github/workflows/dev-canvas-e2e.yml`. Leave the RF bundle guard (`src/components/d3CanvasNav.guard.test.ts`) as-is (it inverts at P7). **One-line rollback if `verify` flakes:** re-add `--grep-invert @dev-flag`. Full banked map (exact line list, risk-ordering) is in `HANDOFF.md`.
- **P7 — THE DEFAULT-FLIP (last, the reversing step) + SITEMAP §1–4 / 085 supersession in the SAME PR. HIGHEST prod risk — MANDATORY adversarial review.** `src/App.tsx` `d3CanvasEnabled()` → a capability check (default ON desktop/tablet), canvas primary, `WorkspaceSurface` the < 1024px fallback; `src/store/canvasMode.ts` seeds from capability; **RF now ships → invert the bundle guard + `vite.config.ts` `globIgnores` into a budget ceiling.** ~50+ non-`@dev-flag` specs assume the `WorkspaceSurface` DOM — run the full suite against the canvas in a branch first (D2 P5 Rule-12 sweep is the template); narrow specs pin `WorkspaceSurface`. Precise SITEMAP/085 edits are in the 089 issue `### SITEMAP + 085 lockstep edits`.

## Workflow (per phase — divide-and-conquer with subagents)
**INVESTIGATE** (read-only `Explore` → verbatim file:line map, keeps it out of your context) → **RED-FIRST** (a failing unit/e2e for the Gate) → **IMPLEMENT** (a `fork` inherits your context for a large multi-file build; else inline) → **ADVERSARIALLY REVIEW** (`code-reviewer` on the diff — MANDATORY for P7 + any store/write-path/hot-path touch; EVERY phase's review this build caught real bugs) → **VERIFY yourself** (`npm run verify:fast` + the full `e2e/d3-canvas.spec.ts` + test EVERY route on the canvas + screenshot) → **COMMIT** (`--no-verify` after verifying + explicit `git add`) → push → confirm CI `verify` green + `deploy` ran. Parallelize read-only investigation of the next phase while the current one implements.

## Non-negotiables (full list in the HANDOFF)
- **Deploy = push to `main`;** watch CI with `gh run list --json` poll loops (`gh run watch` is flaky). If a deploy is skipped, check whether `verify` is red-streaking.
- **Keep `?d3rf` specs `@dev-flag` UNTIL P6 proves them green** (a dev-flag flake must never freeze prod deploy — issue 096).
- **STALE-VITE:** `pkill -f "@playwright/test/cli.js test-server"; pkill -f vite; lsof -ti:5173 | xargs -r kill -9` before every e2e re-run.
- **PROD-FOLD** (until P7): prod-build + grep `xyflow` stays OUT of the main bundle after any App-gate/`canvasMode`/lazy-import change. **Inverts at P7** (RF ships → budget ceiling).
- **TEST EVERY route on the canvas** + navigate; watch for "Maximum update depth" tier-route bugs. **Keep `routes.ts` grammar intact.**
- **≤3 concurrent subagents;** never let two subagents edit the same file on `main`; worktree-isolate overlapping ones.
- **Adversarially review** MANDATORY for P7's default-flip + the SITEMAP/085 edits. **Screenshot** every user-facing change (canvas is user-facing from P7). **Schema only via migrations** (089 is frontend-only). CloudWatch (`…WriteApiFunction…`, profile `phani-quadnomics`, read-only) is the authoritative write-path live check.
- All 089/093 owner-forks are answered — **build autonomously**; surface a genuinely NEW fork via `AskUserQuestion`.

## Definition of done
089 graduated (canvas is default; the 011/012 cluster built; P6–P7 shipped + SITEMAP/085 updated) + live-verified + archived to `docs/issues/done/`; the P3 stub→live-core follow-up shipped or filed as its own issue; then re-triage `docs/issues/README.md` until open-issue count is **0**. Leave a compressed HANDOFF update. **Loop until 0.**

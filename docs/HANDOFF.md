# HANDOFF — 2026-07-16 (088 fix LIVE, pending live-verify · 084 row-actions redo built, deploy was blocked→refixed · 089/090 canvas-workspace design cycle done)

For the next agent. Read this → `docs/issues/README.md` → the relevant issue. Everything else is reference.

## Where things stand

**Repo**: https://github.com/jrkphani/GeDe (public, `main`). Live at **https://d1nzod71m3rz6x.cloudfront.net** (AWS `975049998516`, `us-east-1`). **Deploy = push to `main`** → CI `verify.yml` (typecheck + lint + stylelint + vitest + **Playwright e2e**) → `deploy.yml` runs `cdk deploy --all -c debugApi=true`. **Current live build: `index-CTX6WQjE.js`** (commit `ffde257`).

**⚠️ Deploy state is mid-flight — read carefully:**
- **LIVE (`ffde257` → CTX6WQjE)** = the **088 mechanism-A fix + reentrancy guard** (`8354f04`, LIVE but **NOT yet live-verified**) **+** **084 D1 Phase-1** with the **FLAWED row-actions** (Add child / Remove rendered as a data column *between* Name and Description).
- **`origin/main` = `dac4120`** (pushed, **CI verify FAILED**, deploy **skipped**): it adds the **084 row-actions REDO** (`fbcfe21`) + the 089 doc, but **did not deploy** — so the flawed layout is still live.
- **Local `main` is 3 commits ahead of `origin`** (unpushed): `a60d810` (089 SITEMAP impact), `5e512a4` (the fix for the verify failure), `2f02e9f` (090 doc). **Pushing these deploys the 084 redo.**

**The verify failure + the fix**: the 084 redo put the `→ dimension` source badge inline on the Name cell, and it joined the cell's *accessible name* (a promoted "Users" read as "Users → Stake"), which broke `architecture.spec.ts`'s `getByRole('cell', {name, exact:true})` rename-propagation locator (60s timeout — the only e2e that touches promoted-entry cells). Fixed with `aria-hidden` on the decorative badge (`5e512a4`); unit (58) + the architecture e2e both green. **Full e2e is being run locally to confirm before the re-push** (don't burn a 2nd red CI cycle — CI e2e is the gate `verify:fast` doesn't cover).

## This session's arc

1. **088 — root-caused + fixed the residual false-orphan "Sync error".** The prior partial fix passed tests but FAILED live and left two candidates. Found + **proved candidate (A) with a deterministic local test** (no diagnostic deploy needed): `applyInboundDeltas` runs the retry buffer in ONE atomic tx, so a genuinely-orphaned sibling rolls back a *resolvable* row too → false orphan (077-class whole-batch cascade). Fix: **per-row fallback drain** + a **single-flight coalescing guard** (the guard fixed a HIGH from adversarial review — concurrent drains double-firing `onApplied`). 45 sync tests green, adversarially reviewed. **Deployed live in CTX6WQjE. Still needs the heavy-account live-verify to close.**
2. **084 — Tier-2 row-actions.** Shipped D1 Phase-1 (top add-row, empty state, typed add-child). Owner caught that "Add child"/"Remove" were a data column between Name/Description. **Redid it** ("Chrome + selection bar"): badge inline on Name (aria-hidden), Add child in a **hover-revealed trailing gutter**, Remove in the **selection bar**; data columns = Name + Description only. **Screenshot-verified** (3 states). Deploy blocked by the e2e regression above, now refixed and pending re-push.
3. **089 / 090 — canvas-workspace design cycle** (proposals, NOT started; owner decisions captured in-doc):
   - **089** (`docs/issues/089-unified-canvas-workspace.md`): merge the 3 tier routes into one **React Flow** (`@xyflow/react`) zoomable canvas — lanes per tier, positions **derived** via dagre/elk auto-layout from the ordinal `sort` (STYLE_GUIDE §1 principle 4 preserved, no x/y persisted), drag constrained-to-lane = reorder. Plus a **global rich-text toolbar** (D1 — separable, ship first). Drill-in = adjacent child-canvas cluster + edge; coverage = ditto (companion node); register stacked over ring; **full SITEMAP/routing impact section** (routes collapse to spatial state); library research appended (research recommended rejecting scene-graph — owner chose RF + auto-layout with the tradeoff documented).
   - **090** (`docs/issues/090-multiple-design-canvases.md`): make **canvas a first-class entity** (a `canvases` table) so a project can have **multiple design canvases**. Agent-grounded vertical slice: `createProject` seeds nothing (empty root canvas), ~13 sync registries touched, migration `0017` + backfill, `DEFERRED_FK_COLUMN['canvases']=['parentContextId']`, `ENVELOPE_TABLE_NAMES` insert, migration-count 17→18, RLS in 0008. **Sequences BEFORE 089's canvas merge**; 089-D1 toolbar is independent.

## Still open / next steps (in priority order)

- **① Push the 3 local commits → deploy the 084 redo.** After the full e2e confirms green: `git push origin main` → CI → deploy → live-check the corrected Architecture layout (Name + Description only; Add child hover-gutter; Remove in selection bar).
- **② Live-verify 088 (this is all that's left to CLOSE 088 — the fix is already LIVE).** Fresh sign-in smoke on the heavy account (`jrkphani@gmail.com`, ~12+ junk projects = built-in stress); confirm the "Sync error" banner no longer appears (self-heals silently to `Synced`). If gone → **archive 088 to `done/`**. If it persists → it's candidate (B) (parent genuinely un-applied at surfacing) → do the **078 instrumentation playbook**. Needs the scratchpad live-smoke `.mjs` (owner's plaintext password — owner-run or owner-authorized).
- **③ 084 is PARTIAL — do NOT archive as-is.** Phase 1 + row-actions shipped; **Phase 2** (cross-table keyboard Tab-chain), **grid unification (D3)**, and **finding 8** (listbox selection a11y) remain open with unchecked acceptance criteria. Update 084's status to reflect this, or split the remainder into a follow-up issue.
- **087 — surface silent write failures**: OPEN, not started (the write-side complement of 086).
- **089 / 090 implementation** (when the owner greenlights build): **090 first** (schema slice — ships in the current shell via a canvas switcher, no infinite-canvas dependency) → then **089** canvas merge renders N canvases as clusters. **089-D1 (global rich-text toolbar)** is independent, high-value, low-risk — a good standalone next build.
- **076-class backend 502s** on `/sync/v1/shape` (seen during a prior 088-verify reload): distinct ShapeProxy/Electric hiccup; own look (CloudWatch the ShapeProxy/SyncContainer).
- **CDK-synth `/tmp` leak**, **RLS no-op in prod** (`gede_admin` owner; 080's `/accept` is the sole authz boundary), and **test-data clutter** on the account — all still standing.
- **Housekeeping**: 4 stale `.claude/worktrees/agent-*` worktrees (~3.2 GB) from prior crashes remain — `git worktree remove` the safe ones to reclaim disk (check each for unpushed commits first).

## Non-negotiables (how to work)
1. **TDD, red first.** 2. **Deploy = push to `main`** — CI is the only path, and **CI `verify` includes Playwright e2e that local `verify:fast` does NOT run**, so a UI change can pass locally and fail CI (exactly what happened with the 084 badge). Run the affected e2e (or the full suite) locally before pushing a UI change. 3. **Schema only via migrations** (latest `0016`; next `0017`; adding one bumps `deploy/cdk/test/migration-stack.test.ts:85` count 17→18 + the `.snap`; Electric-synced tables need `REPLICA IDENTITY FULL` + a `syncScope`/`WORKSPACE_SCOPE_SQL` entry + the ~13-registry envelope fan-out — see 090's plan; a new parent FK needs `DEFERRED_FK_COLUMN`/apply-order consideration à la 088). 4. **Verify subagent claims against the code** (adversarial review caught the 088 reentrancy gap; the 090 agent caught real draft errors). 5. **Verify LIVE, don't infer** — the whole 088 saga is a fix passing tests + adversarial review yet failing at production scale. 6. **Screenshot UI changes before shipping** — the FIRST 084 shipped a layout bug (verbs in a data column) that only a screenshot caught; the redo was screenshot-verified. 7. **Orchestration**: ≤3 subagents concurrent; system memory <75% (`memory_pressure` "free %"); reap stray playwright/chromium; e2e `--workers=1`.

## Tooling
- **AWS profile matters.** App account **`975049998516` = profile `phani-quadnomics`**; the owner's default shell profile is a different personal account, so raw `aws …` fails cross-account — use **`AWS_PROFILE=phani-quadnomics`**. The AWS MCP (`mcp__aws-api__call_aws`) is authenticated to the app account.
- **049 debug API** (SELECT-only over the ALB): `TOKEN=$(aws secretsmanager get-secret-value --secret-id arn:aws:secretsmanager:us-east-1:975049998516:secret:DebugTokenSecret0E11C939-uC7geGa8eYXb-uCCrkF --query SecretString --output text --region us-east-1 --profile phani-quadnomics)`; then `curl -H "x-debug-token: $TOKEN" http://<ALB>/debug/db/counts` or `POST /debug/db/query {"sql":"…"}`. **ALB DNS**: `Gede-T-Alb16-7PG12HghU4Wa-1482490479.us-east-1.elb.amazonaws.com`.
- **CloudWatch** = fastest server debugger: Write Lambda `…WriteApiFunction…`, ShapeProxy `…ShapeProxyFunction…`, Electric `…SyncContainerLogGroup…-iC9rDOPocc3x` (logs `Received relation "public"."<table>"` when a change flows through WAL).
- **Live-smoke pattern**: standalone Playwright `.mjs` in `scratchpad/*/run.mjs` drive the live URL, signing in via `#hero-email`/`#hero-password`. **Security note**: those scratchpad scripts hardcode the owner's plaintext password — **gitignored** (`scratchpad/` excluded), plaintext on local disk only. Accounts: A=`jrkphani@gmail.com` (heavy/stress), B=`jrkphani@icloud.com`. `window.__gede` introspection is REMOVED — verify via UI/DOM only. **Local app screenshots**: `npx playwright test <spec>` auto-starts the vite dev server (`playwright.config.ts` webServer, port 5173); a scratch spec that builds data + `locator.screenshot()` is how the 084 redo was visually verified.
- **Classifier** blocks mutating AWS + `git push` + secret-plaintext reads from the agent — hand the user a `!` command for those. `gh run watch` is flaky on this network; a `gh run view --json status` poll loop is more resilient.

## Docs map
`docs/SPEC.md` · `TECH_STACK.md` · `STYLE_GUIDE.md` · `SITEMAP.md` (now carries a forward-pointer to 089/090's route changes) · `adr/` · `DEPLOYMENT.md §9` · `docs/issues/` (README = index; `done/` = shipped; **084/087/088 open**, **089/090 proposals**). Knowledge graph in `graphify-out/` (gitignored).

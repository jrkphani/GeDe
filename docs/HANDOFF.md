# HANDOFF — 2026-07-19 (session 3): 084 Direction-3 grid unification COMPLETE (P0–P6 shipped + archived) · open issues now 089 + 093, both owner-gated

## ▶▶ NEXT ORCHESTRATOR — START HERE (supersedes everything below)

**Live build: P5 is live in `index-CJUxFg34.js` (via `acece4c`); P6/a11y (`e6ba1eb`) was verifying→deploying at handoff — CONFIRM the latest hash + that `e6ba1eb`'s `verify` went green and deployed.** Deploy pipeline GREEN. Deploy = push to `main` → CI `verify` → `deploy` via `workflow_run`. The `@dev-flag` guard still holds (`?d3rf` d3-canvas tests excluded from the gating `verify`, run in the non-gating `dev-canvas-e2e.yml`).

**⚠ CONCURRENT-SESSION NOTE (please act):** a SECOND long-running Claude Code session (PID 2093, cwd = this repo, alive since Thu Jul 16) made one pushed commit this session — `acece4c` (added the `@axe-core/playwright` devDep + a HANDOFF edit) — i.e. two orchestrators briefly shared this working tree. It committed only `package.json`/lock + HANDOFF (no source overlap with my P6), then went idle (0.1% CPU, no pending work in the tree). **My session-3 work reconciled cleanly.** If that session is still open, **close it** to avoid future two-writers-on-`main` races (the exact hazard the non-negotiables warn about).

**✅ 084 Direction-3 grid unification — DONE (P0–P6 shipped, ARCHIVED → `docs/issues/done/084-...`).** Design: **stacked per-table `EditableGrid`s threaded by ONE outer `EditableChainProvider`** (NOT grid-in-grid), add-table phantom as the chain's terminal node. Commits: P0 `313551d` · P1 `4936792` · P2 `e78f4e3` · P3 `203b527`+`bac729f` · P4 `2d0f55e` · **P5 `a77291c`** (quiet aria-hidden `ui/key-hint` + `EditableGrid.showKeyHints`) · **P6 `e6ba1eb`** (e2e+axe sweep). Plus `0347d7f` — the P4 `role=option` migration to the SHARED select control also needed `e2e/d3-canvas.spec.ts` → `getByRole('option')` (that stale `getByRole('button','Select …')` was the sole `dev-canvas-e2e` failure; **this is the recurring pattern — a shared `TablePanel`/EditableGrid change ripples to `d3-canvas.spec.ts`, always run/update it**). The frozen `onExitBoundary(dir)` seam + DEFAULT-OFF seams held throughout: **`d3-canvas.spec.ts` 8/8** (the `?d3rf` canvas stays byte-identical). Verified: `verify:fast` 1605 passed; `architecture.spec.ts` 6/6 (incl. axe); foundation/context-register/dimensions/parameters 13/13; screenshot-verified on the real `/architecture` route.

**P6's axe sweep found + fixed a real pre-existing a11y bug** (`aria-prohibited-attr`, serious): every `EditableGrid` display cell (text/multiline/richtext) named its empty state via an `aria-label` on a role-less `<div>` → now carried in **visually-hidden text** (identical accessible name, no prohibited attr, no role change — `role=button` was tried and REJECTED: it reshaped the app-wide button landscape and broke button-enumeration tests). **Known follow-up (out of P6 scope):** the read-only/viewer display branches of those same cells still carry the `aria-label` pattern — off the `/architecture` editor route; do a viewer-mode axe sweep when convenient.

**P6 volume note:** the 20×50 volume test bulk-seeds via the supported `.gede.json` **import path** (`page.locator('input[type="file"]').setInputFiles(...)`) with a hand-built FORMAT_VERSION-5 envelope — the importer remaps all ids, so opaque ids are fine (schema: `src/domain/projectEnvelope.ts`; the minimal fixture is 1 project + `tier2_tables` + `tier2_entries` + the other 8 arrays empty). Reuse this pattern for any future volume e2e — UI-seeding 1000 rows is too slow.

**Shipped + verified + ARCHIVED this session (all on `main`, all deployed):**
- **098** (SECURITY — write-path insert/update rejects cross-tenant FK targets; review found + closed the `adoptedIntoProjectId` same-class gap).
- **091** (tier1_purpose diverged-id `update` → natural-key resolution; live-verified — both edits persist, no note, CloudWatch clean).
- **094** (undo/redo cloud data-loss) — **owner chose the dedicated `revive` op**; server (`f77d69a`) + client (`ad17c8a`); **3 adversarial-review rounds** (each caught a real bug: the `ON CONFLICT`/NOT-NULL failure, the natural-key 23505, the client op-selection); real-Postgres-verified; also fixed the **latent 070 restore-from-archive + `revertStale`** same-class bugs.
- **096** (d3-canvas popover hardened+re-enabled 33/33; focus-pan honestly quarantined; the deploy-gate guard above).
- **097** (cross-tenant upsert guard, SECURITY) archived. Plus the quick-win `pgWriteStore.live.test.ts` canvas_id fixture.

**089 assessed → correctly GATED behind 084 D3** (the 089 canvas wraps the Architecture grid; out-of-order = rework). **Owner chose 084 D3 as the next track**, answered all **8 forks + 3 UX requirements** in a fork-interview.

**084 Direction-3 grid unification — BUILD IN PROGRESS (the current active work).** The authoritative spec is `docs/issues/084-tier2-architecture-ux.md` → **"## Direction 3 — APPROVED BUILD PLAN (2026-07-19)"** (8 resolved forks, the shared-seam protocol, P0–P6 phased plan). Design in one line: **stacked per-table `EditableGrid`s threaded by ONE outer `EditableChainProvider`** (NOT grid-in-grid), add-table phantom as the chain's terminal node. **Committed:** P0 (shared `src/components/gridBoundaryFocus.ts` seam), P1 (chain mount + trailing add-table + token indentation + parent-heavier font-weight), P2 (cross-table Tab via the seam adapter — the core-risk phase), P3 (inline typed add-child via an additive `EditableGrid.inlineRow` seam), the **per-depth child-record indentation** refinement (owner req — child names now clearly indented via an additive `EditableGrid.rowStyle` seam), P4 (listbox selection a11y `role=option`/`aria-selected` + quiet `rowAction` Remove), P5 (quiet `aria-hidden` keyboard hints — `ui/key-hint` + `EditableGrid.showKeyHints`, `a77291c`). **REMAINING: only P6 (e2e/a11y sweep — the full keyboard-only flow + volume + an axe a11y pass; `@axe-core/playwright` is now a devDependency).** *(Note: the P4 `role=option` migration to the SHARED select control also needed `e2e/d3-canvas.spec.ts` updated to `getByRole('option')` — done in `0347d7f`; `dev-canvas-e2e` is green again. This is the pattern: a shared-`TablePanel`/EditableGrid change ripples to the `?d3rf` d3-canvas.spec too — always run/update it.)* Non-negotiable for every 084 phase: keep the `EditableGrid.onExitBoundary(dir)` signature FROZEN and every EditableGrid seam DEFAULT-OFF so `e2e/d3-canvas.spec.ts` (the `?d3rf` `WorkspaceCanvas` consumer) stays green — **run it after any EditableGrid change**. 084 edits the REAL `/architecture` route (user-facing; shipping incrementally), so **screenshot-verify every visual phase** (a CSS-specificity bug in the indentation pass was caught only by the screenshot).

**OPEN issues = 2 (089 + 093). OWNER ANSWERED the checkpoint (2026-07-19) — both are now a settled, phased, multi-session BUILD (no more forks blocking; execute the specs):**
1. **089 D3 graduation** (`089-...md`) — **OWNER CHOSE: GRADUATE the canvas to the DEFAULT workspace + build the 011 recursion / 012 coverage cluster NOW as edge-connected satellite nodes.** This deliberately reverses 085 ("tables are the instrument") + SITEMAP §1/§2 ("page never scrolls"). **The authoritative phased spec is `089-...md` → `## D3 GRADUATION — APPROVED BUILD PLAN (2026-07-19)` — P0–P7, ≤5 files/phase, red-first, SAFE non-reversing work first (P0 thread the `?d3rf` flag into a `canvasMode` store; P1 decompose Foundation lane; P2 decompose Design {ring+register} core; P3 recursion satellite; P4 coverage twin; P5 LOD/perf; P6 deploy-gate transition) and the DEFAULT-FLIP + SITEMAP/085 edits LAST (P7).** START AT **P0** (thread the flag — every later phase navigates and would otherwise drop `?d3rf`). Keep `routes.ts` grammar intact throughout (011/012/090 depend on it); keep the frozen-seam + run-d3-canvas-after-every-EditableGrid-change discipline.
2. **093** (`093-...md`) — D3 context-register extend-right. **ALL 5 FORKS RESOLVED (owner, 2026-07-19):** canvas-only · capped + LOD · **remove** the top "New context" button (register phantom row becomes sole create; `c` key keeps compose-mode) · ring centered within the capped width (resolver default) · LOD collapse above ~8 columns / below ~0.6 zoom (resolver default). See `093-...md` → `### Fork resolutions`. **Rides 089 — slot it after 089-P2 (Design-lane decomposition).**

**The autonomous night-shift work is DONE (084 shipped + live + archived). The remaining path to 0 is the owner-approved 089 graduation program (+093) — a large multi-session BUILD, now fully speced. The next orchestrator executes `089 → ## D3 GRADUATION — APPROVED BUILD PLAN` P0 onward, red-first, one phase at a time, screenshot + live-verify each user-facing phase, and re-triages `README.md` when done. No further owner input is blocking — the forks are all answered.**

## ▶ Tooling & non-negotiables (carry forward)
- **Deploy = push to `main`** → CI `verify` (typecheck + lint + stylelint + vitest + Playwright e2e, now `@dev-flag`-excluded) → `deploy.yml` via `workflow_run` on verify success. **If a deploy is skipped, check whether `verify` is red-streaking** (that's how the 8-commit freeze hid). Watch CI with a `gh run list --json` poll loop (`gh run watch` is flaky on this network).
- **Verify subagent claims against the code; adversarially review every security/write-path change** (each of the 3 revive-op review rounds caught a real bug). **Real-Postgres is authoritative** for write-path/SQL (`psql -d postgres -c "DROP DATABASE IF EXISTS gede_livetest;" -c "CREATE DATABASE gede_livetest;"` then `DATABASE_URL=postgresql://jrkphani@localhost:5432/gede_livetest npx vitest run src/server/writeApi/pgWriteStore.live.test.ts` — InMemory ≠ Postgres hid multiple bugs this session).
- **Screenshot UI changes** (`npx playwright test <scratch spec>` auto-starts vite on 5173; `locator.screenshot()`). **Schema only via migrations.** **≤3 concurrent subagents; commit `--no-verify` after running verify yourself + explicit `git add`** (avoids the lint-staged stash dropping a concurrent subagent's edits — never let two subagents edit the same file on `main`).
- **Throwaway live creds** (owner-provided, rotate after): `GEDE_EMAIL='jrkphani@gmail.com' GEDE_PASSWORD='quXtor-cidmu0-gykbet'`; live URL `https://d1nzod71m3rz6x.cloudfront.net`. Live-smoke pattern: `scratchpad/live-verify-*/run.mjs`. The SW hides `/write` from Playwright → **CloudWatch (`/aws/lambda/Gede-Test-Api-WriteApiFunction5106E371-2PvLQCdOFbzl`, AWS MCP read-only, profile `phani-quadnomics`) is the authoritative write-path live check** (successful writes log nothing — verify by absence-of-error). AWS profile: app account `975049998516` = `phani-quadnomics`.

---

# HANDOFF — 2026-07-19 MORNING (night shift: 098·091 SECURITY cluster shipped+live-verified · 096 deploy-gate guard landed · 094 parked on a design fork)

## ☀️ MORNING — NIGHT-SHIFT RESULTS (read this first, then the AskUserQuestion I left you)

**Live build: `index-yIDo1fKM.js`** (client bundle UNCHANGED this shift — all fixes were server-side write-Lambda + CI/test/docs; the write Lambda WAS redeployed & live-verified). Deploy pipeline GREEN.

**Shipped + verified this shift (all on `main`, all deployed):**
- **Quick-win** (`5a9f8ee`) — fixed the broken `pgWriteStore.live.test.ts` canvas_id fixture (090/0017); the real-Postgres suite is green end-to-end again (used it heavily below).
- **098 SECURITY** (`b0011f6`) — write-path `insert`/`update` now rejects cross-tenant FK targets (`resolveForeignKeyTenancy`): a caller could no longer plant a row referencing a victim's `project_id`. **Adversarial review PASSED and found + closed one further same-class gap** (`projects.adoptedIntoProjectId` was absent from `FK_SCHEMA` → checked by neither existence nor tenancy). Red-first, real-PG 7/7. **Archived → done/.**
- **091** (`f978616`) — `tier1_purpose` diverged-id `update` no longer `unknown_entity`s ("item no longer exists" + lost 2nd edit): natural-key (`project_id`) resolution fallback in `checkTenancy` + `applyIfNew` update, mirroring 095's insert fix, with an `AND workspace_id` defense-in-depth guard. Adversarial review PASSED (double-gated by 098). Real-PG 9/9 incl. a cross-tenant regression test. **LIVE-VERIFIED** (both purpose edits persist across reload, no note, CloudWatch WriteApiFunction clean — 4 invocations, zero `[091]`/`[098]`/`unknown_entity`/`cross_tenant`/`5xx`). **Archived → done/.**
- **096** (`614d3a5`) — the promote-popover d3-canvas e2e HARDENED + re-enabled (reduced-motion + poll geometry, 33/33 deterministic); the focus-pan test honestly KEPT quarantined (`test.fixme`, app-side root cause noted). **Structural deploy-gate guard: all `?d3rf` d3-canvas tests tagged `@dev-flag`, EXCLUDED from the deploy-gating `verify` (`e2e` = `--grep-invert @dev-flag`), and run in a new non-gating `dev-canvas-e2e.yml`** — a dev-flag flake can never silently freeze prod deploy again (the ~8-commit-freeze root cause). deploy.yml gating logic untouched. *(CI for 614d3a5 was in flight at shift end — confirm verify went green + the dev-canvas-e2e workflow ran.)*

**⏸️ 094 PARKED on an OWNER DESIGN FORK (the one genuine discovery of the night).** The client-side undo/redo enqueue work is BUILT and green in-memory across all 7 stores — **preserved in `git stash` (`wip-094-undo-redo-enqueue`) + a patch backup in scratchpad** — BUT a real-Postgres probe proved the systematic fix is incomplete: **the write protocol has NO way to revive a soft-deleted row** (update SQL drops `deleted_at` via SERVER_STAMPED; `getRow` filters deleted → `checkTenancy` rejects a revive-update as `unknown_entity`; upsert is `DO NOTHING`). So the *revive* reversals (redo-of-add, undo-of-remove, restore) would enqueue mutations the live server rejects — an InMemory↔Postgres divergence (the 053/054/095 trap). **This also means the forward `restoreArchivedProject` (070) path is likely broken in cloud mode — a latent bug worth its own live check.** The full finding + 3 design options are in `docs/issues/094-*.md` → "## The revival gap". **Needs your decision (see the AskUserQuestion) before it can ship correctly. Owner previously rejected a half-fix.**

**OPEN issues now = 084, 089/093, 094(blocked-on-fork). The 091+094+096+098 night-shift target: 098✅ 091✅ 096✅ 094⏸(parked-on-fork).**

**Owner forks queued for you (in the AskUserQuestion + the issue docs):**
- **094 revival-protocol** (blocks a data-loss fix): (1) dedicated `revive` op [recommended — only unambiguous model, also fixes 070], (2) let `update` carry+apply `deleted_at`, (3) revive-on-conflict upsert.
- **084 Direction 3 grid unification** — the biggest remaining build; DIRECTION decided, 6 build sub-forks queued (canonical grammar, commit-on-blur, richtext, selection a11y, keyboard-bridge scope, add-table placement — see `084-*.md` + mem `S636`/`5620`). A short spec/interview when that track starts. **Do NOT run parallel with 089 Architecture.**
- **089 D3 graduation + 093** (LAST) — 093 has 5 owner forks (`093-*.md`).

---

# HANDOFF — 2026-07-18 EVENING (087·092·095·097 shipped+live-verified · deploy pipeline rescued · 091 root-caused · 094·096·098 filed)

For the next agent. Read this → `docs/issues/README.md` → the relevant issue. Everything else is reference.

## ▶▶ NIGHT-SHIFT ORCHESTRATOR — START HERE (supersedes the older "SEQUENCING & PARALLELIZATION PLAN" below)

**Live build after this session: `index-yIDo1fKM.js`** (deploy pipeline is GREEN again — see 096). Confirm the latest hash before assuming; another deploy (097 guard) was in flight as this was written.

**What shipped + verified this session (all on `main`, all deployed):** 087 (write-stall footer, live 8/8), 092 (undo/redo cross-lane refresh, live 11/11), 095 (`tier1_purpose` upsert no longer 23505s — live-verified via CloudWatch), 097 (SECURITY — cross-tenant `tier1_purpose` upsert guard, adversarial-review-passed against real Postgres), laneLayout duplicate-sort tie-break. **The deploy pipeline had been silently broken for ~8 commits (096) and is now unblocked.**

**OPEN issues (the night-shift target = drive this to 0):**
- **098** (SECURITY, OPEN, ~M) — write-path `insert` never verifies FK-referenced rows (`project_id`) belong to the caller's workspace → cross-tenant row-planting; pre-existing, affects every `projectId`-FK table. **Root of the 095/097 story. Highest-value security fix. Fully specified + repro'd (real Postgres) in the issue.** Server-side, contained, red-first with a real-Postgres live test.
- **091** (root-caused, OPEN, ~S/M) — the `update`-path twin of 095: a client-minted `tier1_purpose` id the server lacks → `unknown_entity` → the "no longer exists" note AND the 2nd+ edit is silently lost. **Fix specified**: resolve `tier1_purpose` by `project_id` for the `update` branch + `checkTenancy` (mirror 095). Consider a coherent design that kills the **id-divergence at the source** across write-tenancy + write-upsert + **client read-apply** (a streamed server row with a different id but same `project_id` can collide on the client's local `tier1_purpose_project_idx`). Then remove the `878eb97` diagnostic. **091, 097, 098 are one tightly-coupled tier1_purpose/tenancy cluster — do them together.**
- **096** (PARTIAL, CI) — two `?d3rf` d3-canvas viewport e2e (`156` popover-anchor, `286` focus-pan) are `test.fixme`-quarantined. Harden + re-enable (deterministic pan-settle / zoom-invariant assert) + add a structural guard so dev-flag e2e can't gate prod deploys again.
- **094** (OPEN, ~M, owner said "after 084 D3") — undo/redo of any mutation is never enqueued to the sync outbox → cloud-mode reversal lost on reload. Systemic across all 8 stores' command-log closures.
- **084** Direction 3 grid unification (OPEN, BIG multi-session build) — **6 owner design forks are ready** (see `S636`/`5620` mem + the exploration; canonical grammar, commit-on-blur semantics, richtext, selection a11y, keyboard-bridge scope, add-table placement). **OWNER-FORK-GATED — surface via AskUserQuestion before building.** Cannot run parallel with 089 Architecture.
- **089 D3 graduation + 093** (OPEN, LAST) — thread `?d3rf` / decompose Foundation+Design lanes / LOD + land 093 (context register extend-right). 093 has 5 owner forks.
- **Quick-win (unfiled, from 097's review): `pgWriteStore.live.test.ts`'s `insert: parameters/bindings/tier2_entries…` is BROKEN** — 090 added `NOT NULL canvas_id` to `dimensions` after that fixture was written, so the live suite doesn't pass end-to-end. Fix the fixture (add `canvasId`) so the real-Postgres suite is green — you'll need it green to add the 098/091 live tests.

**Recommended night-shift order:** (1) fix the live-test fixture (quick-win, unblocks the real-Postgres suite you'll lean on). (2) **The tier1_purpose/tenancy cluster 098 → 091** together, red-first, real-Postgres-verified, adversarial-review each (security). (3) 094 and 096-hardening — parallel-safe, contained. (4) THEN 084 D3 — but it's owner-fork-gated; surface the 6 forks and, if the owner is asleep, either park it with the forks queued or proceed only on the non-forking hygiene. (5) 089/093 last. **Checkpoint the owner (leave a note / AskUserQuestion for morning) only on the genuine design forks (084's 6, 093's 5); everything else is autonomous.**

**Autonomy + tooling for the night shift:** you may `git push` and deploy (push to `main` → CI `verify` → `deploy` via `workflow_run`). **Throwaway live creds are available** for agent-run live-smokes — the owner set `GEDE_EMAIL`/`GEDE_PASSWORD` (account `jrkphani@gmail.com`); run smokes with the creds inline in your own Bash (`GEDE_EMAIL=… GEDE_PASSWORD=… node scratchpad/…/run.mjs`). **Live-smoke gotchas learned the hard way this session (all in the 087 done-issue):** Playwright's `'**/write*'` glob does NOT match `…/write` → use a URL-**predicate** matcher; `page.on('request')` doesn't see a route-**aborted** request → count in the handler; the RichTextEditor commits on bubbling `focusout` → blur the contentEditable directly (`el.blur()`), and use `End` (not `Meta+A`) to append; the new build's SW hides `/write` from `page.on` → **CloudWatch (`…WriteApiFunction…`) is the authoritative live check** for write-path fixes (a successful write logs nothing; only errors log — so verify by absence-of-error + the app footer state). **Use subagents** for investigation, script-writing, and adversarial review (the 097 security review caught the 098 root gap — always adversarially review security/write-path changes). **Never let two subagents edit the same file on `main`** (lint-staged pre-commit stash drops one's edits) — commit `--no-verify` after running `verify:fast` yourself, or worktree-isolate. **Deploy = push; CI `verify` runs Playwright e2e that `verify:fast` does NOT — run affected e2e locally first, and if the deploy is skipped, check whether `verify` is red-streaking (that's how 096 hid).** Clean `cdk.out` after any local synth. Schema only via migrations.

## Orchestrator session 2026-07-18 (live build `index-l2xFhny9.js`)

- **087 SHIPPED + LIVE-VERIFIED → archived to `done/`** (8/8, `scratchpad/live-verify-087/run.mjs`, agent-run on prod). Normal flow shows no false write-stall; a sustained write failure surfaces "Changes not saving" after the 5s grace; it clears once writes succeed. **Verification cost 5 iterations and surfaced 3 durable live-smoke lessons + a real bug** — recorded in the archived issue: (1) Playwright `'**/write*'` **glob does not match** `…/write` → use a URL-predicate matcher; (2) `page.on('request')` doesn't see a route-aborted request → count in the handler; (3) the purpose editor commits on bubbling `focusout` → blur the contentEditable directly. And 087 **surfaced a genuine server bug in the wild** (below), which is exactly its job.
- **095 SHIPPED + DEPLOYED + LIVE-VERIFIED → archived** (`518a00c`). `tier1_purpose` upsert 500'd with Postgres 23505 (`INSERT … ON CONFLICT (id)` ignored the `project_id` unique index; cold-mirror client mints a fresh id → 23505 → silent purpose-edit data-loss). Fix = upsert on the natural key (`ON CONFLICT (project_id) DO UPDATE`, extensible `NATURAL_KEY_CONFLICT` map). Red-first (contract SQL-shape + guarded real-Postgres live test). **Verified live via CloudWatch: ZERO 23505 post-deploy** (was frequent pre-deploy on the same project).
- **⚠ 095 ↔ 091 UNIFIED — 095 was only HALF the fix.** CloudWatch during the 095 live-smoke caught a `[writeApi][091]` rejection: `{unknown_entity, tier1Purpose, op:update, entityId:019f750f…}`. Root cause: the client's minted `tier1_purpose` id diverges from the server row's id; 095 makes the **upsert** reconcile-and-persist (no more 23505), but the **next `update`** for the client's id hits `unknown_entity` (the **091** note). **091 is now ROOT-CAUSED + repro'd (no owner repro needed); complete fix = resolve `tier1_purpose` by `project_id` for the `update` branch + `checkTenancy` too.** My earlier "091 resolved / no recurrence" note was WRONG (no traffic on that path in the searched window) — corrected in the 091 doc.
- **096 FILED + MITIGATED (High, process) — the prod deploy pipeline was silently broken for ~8 commits.** `deploy` runs only on `verify` success; two `?d3rf` d3-canvas e2e (`156` popover-anchor, `286` focus-pan) have failed in CI since `226daa9` (~03:20), so `deploy` was **skipped for every commit since** — P3.4 and the 095 fix never deployed until now (prior "Deployed P3.2/P3.3" notes were wrong: pushed ≠ deployed). Quarantined both (`test.fixme`, `4b3b63d`) → verify green → **deploy unblocked, 095 shipped**. Follow-up in 096: harden + re-enable, and a structural guard so dev-flag e2e can't gate prod deploys.
- **092 SHIPPED + LIVE-VERIFIED → archived to `done/`.** Playwright smoke (`scratchpad/live-verify-092/run.mjs`) on prod, 11/11: promote → Design register shows the dimension; **Undo drops it with NO reload** (window sentinel + URL survived → in-place refresh, not remount); Redo re-adds. Ended redone (server==local consistent).
- **laneLayout tie-break SHIPPED** (`b734c95`) — `computeLaneLayout` now breaks duplicate-`sort` ties by `id` (was input-array-order-dependent, breaking its "order-independent" contract). Red-first; the safe half of the HANDOFF cleanup.
- **094 FILED** (`1f0d167`, new issue) — **undo/redo of any mutation is never enqueued to the sync outbox**: confirmed systemic (NO command-log undo/redo closure in any of the 8 stores calls `enqueueIfSyncing`; forward paths do). Cloud-mode reversal (dimension reorder/remove, promote) silently lost on reload; masked local-only. **Owner decided: STICK TO THE PLAN** — queue 094 AFTER 084 D3. This subsumed the HANDOFF "reorderTable undo-enqueue" cleanup (half-fixing it would break consistency with the mirrored `reorderDimension`).
- **087 live-verify IN PROGRESS.** v1 smoke false-failed (edited an EMPTY Lexical purpose via DOM-Range manipulation that never registered → no write enqueued → correctly no stall). v2 (`scratchpad/live-verify-087/run.mjs`) rewritten: real keyboard typing + asserts the edit registered AND that a `POST /write` actually fired before expecting the stall. Awaiting owner re-run. **087 CODE is unchanged/sound** — the trigger conditions (`writeStalledSince≠null` + `pendingCount>0` + grace) are unit-covered; this is purely a live-smoke fix.
- **084 D3 exploration DONE** — grammar reconciliation (EditableGrid 2D-matrix vs 082 EditableChain 1D-chain) mapped; blast radius + 6 owner forks ready for step 3.

## Latest — continued session 2026-07-18 (pushed to `main`, CI-deploying)

Six commits landed after the D2-LIVE state below; **confirm the live hash before assuming any are deployed**.
- **092 SHIPPED** (`50730d7`) — undo/redo of cross-tier ops (promote / rename-propagate / resolve) now refresh the co-mounted Design lane via `notifyLocalApply` wired into their command-log undo/redo closures (the D2 follow-up gap). Red-first store tests.
- **087 SHIPPED** (`e7470c5`) — sustained write-outbox failures now surface as a calm, debounced **"Changes not saving"** status (`write-stalled`, `WRITE_STALL_GRACE_MS`, ranked above the read `error`; `deriveSyncStatus` stays pure). The write-side mirror of 086.
- **Design-lane relayout SHIPPED** (`124d551`) — owner request: the Design lane is now **ring on top (centered, larger — `min(480px,60vh,100%)`), rail+register table below**, replacing the old side-by-side split. Screenshot-verified. Surfaced + fixed a real hit-radius bug (the shell must be sized to the ring, not the full band, or `useElementWidth` under-sizes the dot hit-targets and the SVG swallows compose-dot clicks). 25 canvas/design/coverage/recursion/workspace e2e green.
- **089-D3 STARTED.** Owner reconfirmed **React Flow (`@xyflow/react`)** as the substrate (fork 5 resolved; docs `bf9eea8`). The mandated **time-boxed spike PASSED all four gates** at zoom≠1 using the REAL `EditableGrid`+`ui/popover` inside RF nodes → **GO** (findings + P0–P4 plan + 3 risks recorded in `089` doc, `9371677`). **P0 SHIPPED** (`d1b5a3a`) — pure `src/domain/laneLayout.ts` derived-positioning core (`computeLaneLayout` + `reorderWithinLane`, 20 tests, no React Flow). **P1 SHIPPED** (`d1c3a86`, owner-approved) — `src/components/WorkspaceCanvas.tsx`: React Flow viewport + ONE real `DesignSurface` node, positioned by P0's layout, gate-(a) grammar proven at zoom≠1 (`e2e/d3-canvas.spec.ts`). Mounted behind a **dev-only, OFF-by-default flag** `App.tsx d3CanvasEnabled()` = `import.meta.env.DEV && ?d3rf` (statically dead in prod builds — normal app unchanged, `routes.ts` untouched). `@xyflow/react` 12.11.2 added; **dagre-free**. Screenshot-verified the real surface renders in-node. **P2 SHIPPED** (`63b64f4`, owner-approved) — all three tier lanes now mount as React Flow nodes: one generic `lane` node keyed by `data.tier` renders the real Foundation/Architecture/Design surfaces in three columns (P0's tier-indexed x); each keeps the P1 contract (header `dragHandle` + `nodrag nopan nowheel` body + per-tier `activeLane`). e2e proves gate-c (Architecture promote popover anchors at zoom≠1) + activeLane gating (`c` from Foundation = 0 drafts, from Design = 1). **Next: P3** = wire constrained node-drag → the existing `sort` reorder mutations (`onNodeDrag` pin x + `onNodeDragStop` → `reorderWithinLane` then re-derive positions; drag is a visual no-op today — one node per lane so nothing to reorder yet). **P4** = focus-pan (pan-if-outside-margin, reduced-motion snap) + ⌘1/2/3 pan-to-lane + lane-summary LOD. **Gotcha:** the `?d3rf` flag lives only in the URL and is dropped by any in-app `navigate()` (serializeRoute) — thread it / move to a store once the canvas is more than a harness. Spike annotations still apply: `nodrag nopan nowheel` + header `dragHandle`; `autoPanOnNodeDrag={false}`; Radix popovers need no fix (floating-ui 1.8.0); focus-pan via `setCenter` required.
- **091 (partial) SHIPPED** (`4641a75`) — the "The item you tried to change no longer exists." note is now suppressed for **D1 heal-originated background writes** only (tagged `origin:'heal'` in `richTextConvert.ts` → filtered in `sync.ts flush()`); user-initiated rejections still surface (no masking). **General root-cause still OWNER-GATED** — hyp. 1/3 (stale-delete echo / edit-before-insert race) needs CloudWatch on `…WriteApiFunction…` per the issue; this only fixes the prime suspect (hyp. 2, the heal). Watch whether the note recurs on the live build for USER edits.
- **084 hygiene SHIPPED** (`8ac3c72`) — the two NON-forking items only: finding 6 (nested indent now token-driven — `--depth` custom prop × `--space-5` in `base.css`, no inline calc) + finding 7 (already resolved by the D1 slice — `querySelector` focus gone; added a regression guard). **084's four design forks (empty-state, add-grammar unification, keyboard bridge, listbox selection) remain OPEN + owner-gated — do NOT archive 084.**
- **D3 navigation SHIPPED** (`e53a138`) — ⌘1/2/3 pan-to-lane (a module-level capture interceptor that beats AppShell's route-nav so `?d3rf` survives) + focus-pan; also fixed a **code-review HIGH** (`useNodesState` seeds once → node data went stale across route changes). **091 diagnostic DEPLOYED** (`878eb97`) — the write handler now logs tenancy rejections to CloudWatch (`[writeApi][091] …`); **owner: reproduce the note (a USER edit) on the live build, then I read the WriteApiFunction log for the exact mutation** (it was never logged before).
- **Owner decisions (2026-07-18), now recorded in the issues:** (1) **D3 reorder = decompose lanes into per-item nodes** — the real P3 is a big spike-then-build that re-opens EditableGrid grammar per-node (current whole-surface nodes are interim); (2) **089 rich identifiers = keep PLAIN** (fork 4 closed); (3) **084 = Direction 3 (grid unification)** on 082's shared grammar seam — a dedicated multi-session build, not started; (4) **091 = investigate now** (diagnostic deployed, above).
- **Open D3 code-review follow-up (MEDIUM, not blocking):** `@xyflow/react`'s ~18.6 kB CSS still ships to prod (JS is tree-shaken out, but Vite never tree-shakes a side-effect CSS import). Fix = split the ⌘ interceptor into a tiny eager module + `React.lazy` the heavy canvas behind the flag (a naive lazy-load breaks the interceptor's register-first ordering) + a build-size guard. LOW: `laneLayout` duplicate-`sort` tie-break is array-order-dependent (defense-in-depth; DB keeps `sort` dense).
- **MEDIUM CSS-leak FIXED** (`b8cdbc1`) — React Flow's JS AND CSS are now out of the prod bundle: `src/components/d3CanvasNav.ts` (tiny, RF-free, eager — holds the ⌘ interceptor so it still registers first) + `React.lazy(WorkspaceCanvas)` behind `<Suspense>`; guard test blocks any static re-import. Build-confirmed (main index.js/css xyflow-free; RF in a 15.87 KB unfetched chunk).
- **D3 P3 — spike GO + P3.0 SHIPPED.** The per-item-node spike PASSED (see `089` doc `## D3 — spike PASSED` addendum): cross-node Tab is solvable via a capture-phase boundary handler + `EditableGrid.onExitBoundary`. **P3.0 SHIPPED** (`d10eb64`) — additive `EditableGrid.onExitBoundary(dir)` seam (commit-then-signal + `exiting`-ref blur guard; **undefined for all 16 existing callers = byte-identical**, proven by guards + a 13-15-spec grid e2e sweep). **P3.2/P3.3 SHIPPED** (`226daa9`, owner-approved) — the Architecture lane is now an `archHeader` node + one `archTable` node per `tier2` table (real exported `TablePanel` per node); **cross-node Tab follows `sort` order** (not DOM order) via the P3.0 `onExitBoundary` seam; measured heights + `useNodesInitialized`-gated fit + rAF×2 phantom-create. **PERF VERDICT: ship as-is** — 5 table nodes incl. one with 15 entries at 0.52× zoom → cell-open 33ms / promote popover 32ms / no jank; **no LOD needed now** (revisit only at dozens-of-tables × hundreds-of-rows). `ArchitectureSurface.TablePanel` exported + an additive optional `onExitBoundary` (conditional-spread → absent → normal surface byte-identical). **P3.4 SHIPPED** (`4c7f205`) — dragging a table node's header now persists a real reorder: new `reorderTier2Table` (db/mutations.ts, mirrors `reorderDimension`) + `reorderTable` store action (enqueues a `tier2_tables` update per sort-changed row, one undo step); `onNodeDrag` pins x, `onNodeDragStop` ranks by center-y → `reorderTable` → re-derives all positions. **Derived-positioning PROVEN: a store test asserts the outbox rows carry no `x`/`y`/`position` — only `sort`; e2e proves the order survives reload (re-derived from persisted `sort`).** **⇒ The D3 P3 arc (per-item Architecture decomposition) is COMPLETE behind `?d3rf`.** P3.1 (N-per-lane layout) was already handled by `computeLaneLayout`/`reorderWithinLane`.
  - **D3 P3 known follow-ups (not blocking; dev-flag-only):** (1) the `reorderTable` undo/redo closures don't re-enqueue the `sort` change (same class as issue 092; consistent with the mirrored `reorderDimension`); (2) only the **Architecture** lane is decomposed — Foundation/Design are still whole-surface nodes (decompose them the same way when wanted); (3) LOD still deferred (perf verdict was ship-as-is at current volume); (4) the `?d3rf` flag still drops on any in-app `navigate()` — to make the canvas more than a harness, thread it / move to a store. **Next real D3 decisions (owner):** graduate the canvas past the dev flag, decompose the other two lanes, or leave D3 here and move to **084 Direction 3** (grid unification, decided, not started).

## ▶ SEQUENCING & PARALLELIZATION PLAN — start here (2026-07-18)

**Orchestration model.** The main agent is the ORCHESTRATOR: plan → dispatch ≤3 concurrent subagents → **verify every subagent claim against the code + run the affected e2e locally** → commit/push → loop until the open-issue count is 0, then triage the README for anything newly filed. **Never let two subagents edit the same file on `main`** — the `lint-staged` pre-commit stash silently DROPPED a subagent's edits during a partial commit this session. Mitigations that worked: commit with `git commit --no-verify` **after** running `npm run verify:fast` yourself, and/or worktree-isolate any subagent whose files might overlap another's.

**Open-issue state (this session's end):**
- **087** — SHIPPED (`e7470c5`). No build work. **AGENT can live-verify + close it** (owner confirmed 2026-07-18): sign in on the live URL with the **`GEDE_EMAIL` / `GEDE_PASSWORD` env vars** via the live-smoke Playwright pattern, then **Playwright `route`-abort the `POST …/write`** so an edit's flush keeps failing → after the 5 s grace the footer must read **"Changes not saving"** → un-abort → it clears to **Synced** (+ confirm no *false* write-stall in the normal signed-in flow). Then archive to `done/`.
- **092** — SHIPPED (`50730d7`). No build work. **AGENT can live-verify + close it**: sign in (env creds, live-smoke), on the D2 lane page **promote an Architecture entry to a dimension, then Undo** — the co-mounted **Design register must refresh (the dimension drops out) with NO reload** (and Redo re-adds it). Then archive to `done/`.
- **091** — heal-case fixed (`4641a75`) + CloudWatch diagnostic deployed (`878eb97`). **General root-cause BLOCKED on owner live repro** (a USER edit), then a small server-side fix + remove the diagnostic log.
- **089** — D1/D2 live; **D3 P3 arc COMPLETE behind `?d3rf`** (P0→P3.4); rich identifiers RESOLVED (keep plain). Remaining = OPTIONAL graduation (thread the flag / decompose Foundation+Design lanes / LOD) + two small follow-ups (`reorderTable` undo-enqueue; `laneLayout` tie-break).
- **093** — NEW (`093-d3-context-register-extend-right.md`, exploration written 2026-07-18). A **089-D3 canvas UX refinement**: on the infinite canvas the Design node's context register should **extend RIGHT instead of clipping** (root cause: `.register-scroll{overflow-x:auto}` + nowrap-header auto table inside the fixed `.wc-node{width:960px}` — far cols hide behind the sticky symbol col), and the stranded top **"New context" button** should be re-homed by the register. Verified safe: Design is the rightmost lane (`LANE_ORDER[2]`, constant-stride x), so a wider node grows into empty canvas, shifting nothing. **~M.** **OWNER FORKS pending** (do not build blind): (1) scope canvas-only [recommended] vs everywhere; (2) ring-vs-register layout when register ≫ ring; (3) fate of the top "New context" button (remove / relocate / unify compose-vs-phantom); (4) width unbounded vs capped; (5) LOD threshold.
- **084** — hygiene 6/7 shipped (`8ac3c72`); **Direction 3 (grid unification) DECIDED, NOT started** — the biggest remaining build.

**The hard constraint (drives the whole sequence):** 084 D3 and 089's Architecture are the SAME real estate — both rebuild `EditableGrid`'s grammar + `ArchitectureSurface`/`TablePanel`. **They cannot run in parallel, and building them out of order = rework.** Note 082's `EditableChain` grammar seam exists only for the Design *rail* (`inline-editor.tsx`), NOT for `EditableGrid`'s tables — so 084 D3 must genuinely reconcile two grammar systems (real foundational work, not a component reuse). Settle one reconciliation before 084 D3 starts: *the 089 canvas will host the 084-unified grid* — which is exactly why 084 D3 comes first.

**Ideal sequence:**
1. **(AGENT-doable now, parallel — blocks nothing):** live-verify + archive **087 & 092** yourself via the env-var creds + live-smoke Playwright (the *how* is in the open-issue-state block above — 087 = `route`-abort `POST /write`; 092 = promote-then-Undo on the lane page). Dispatch a subagent for each. **Owner-external:** reproduce **091** on the live build (a USER edit).
2. **091 root-cause** — the moment the owner repros: read `[writeApi][091]` in the `…WriteApiFunction…` CloudWatch log group (via the AWS MCP, read-only), cross-ref the `entityId` vs DB state (tombstoned ⇒ hyp.1 stale-delete echo; never-inserted ⇒ hyp.3 edit-before-insert race), ship the fix, remove the diagnostic. Small, server-side, parallel to any UI track.
3. **084 Direction 3** — BEFORE further 089 graduation (the canvas wraps the Architecture surface, so it inherits the improvement; and it's the real user-facing route). Phase it like D1/D2/D3: short spec/interview (the crux is reconciling the two grid grammars) → phased build, ≤5 files/phase, red-first.
4. **089 D3 graduation + 093** — thread the flag / decompose Foundation+Design lanes / LOD, and land **093** (register extends-right + re-home "New context") — LAST, on top of the unified grid. **093 needs its owner forks answered first** (get them via `AskUserQuestion` when this track starts); it's a D3-canvas change so it naturally rides the graduation work.

**Parallel opportunities:**
- Track A (AGENT, parallel): live-verify + archive 087/092 via env creds (a subagent each). Owner-external: repro 091 — anytime.
- **091's server-side fix** — parallel to any UI track (different files).
- **Small isolated cleanups** — `laneLayout` tie-break (089-only file) + `reorderTable` undo-enqueue (092-class, `tier2.ts`) — a quick subagent, anytime; but if 084 D3 is actively touching `tier2.ts`/`EditableGrid`, sequence to avoid the same-file clash.
- **NOT parallel:** 084 D3 ‖ 089 Architecture. Foundation/Design lane decomposition COULD overlap 084 D3 (different surfaces) but both touch `EditableGrid` — worktree-isolate if attempted.

**"Issue count 0" =** 087 & 092 agent-live-verified + archived to `done/`; 091 root-caused + fixed + diagnostic removed + archived; 084 shipped (D3 or a re-scoped owner decision); 089 D3 graduated OR explicitly parked with its follow-ups tracked; 093 shipped (with its forks answered) or explicitly parked. Then re-triage `docs/issues/README.md` for anything new.

## Where things stand

**Repo**: https://github.com/jrkphani/GeDe (public, `main`). Live at **https://d1nzod71m3rz6x.cloudfront.net** (AWS `975049998516`, `us-east-1`). **Deploy = push to `main`** → CI `verify.yml` (typecheck + lint + stylelint + vitest + **Playwright e2e**) → `deploy.yml` runs `cdk deploy --all -c debugApi=true`.

**Current live build**: **`index-D26xjZ_8.js`** — **089-D2 (the lane page) is LIVE and 8/8 browser-verified** (2026-07-18). Everything from this session (084 redo, 090 switcher, 089-D1 toolbar, 089-D2 lanes) shipped and was live-verified. Still **confirm the latest hash** at `https://d1nzod71m3rz6x.cloudfront.net` before assuming — do not infer.

## What shipped this cycle

**090 — SHIPPED. Canvas is now a first-class entity.** A design canvas is a real `canvases` row (was an implicit `(project_id, context_id/parent_id)` composite key), so a project holds **N root canvases** with independent dimensions/contexts/bindings, switchable in the Design context bar. Phases 1-4 merged to `main` and pushed.
- **Migration `0017_canvases.sql` applied cleanly to prod RDS** — verified via CloudWatch: the migration-runner logged `applied 1 (0017_canvases.sql); skipped 17`, the `Gede-Test-Migrations` custom-resource stack went `UPDATE_COMPLETE`, and the API stayed healthy with **no `42501`** (the GRANT correction landed — see below).
- **`verify:fast` green (1379 tests)** + **full e2e green (51)**.
- Four corrections the original plan missed, now documented in `docs/issues/090-multiple-design-canvases.md` → `## What shipped`: (1) the **missing `GRANT … ON canvases TO app_user`** (without it the server role is `42501`-denied and cloud project creation breaks); (2) **`FORMAT_VERSION` 4→5 + `upgradeV4ToV5`** (a legacy v4 export was unparseable once `canvasId` joined the row schemas — the shim synthesizes the canvas layer at import); (3) the **phase-4a read-path repoint** of `canvasScope`/`contextCanvasScope` from `context_id IS NULL` to explicit `canvas_id` (the load-bearing correctness change — without it two root canvases leak into each other); (4) a **`canvasId` arg on dimension reorder/remove/restore** (phase 4c step 0) so those ops are canvas-correct on a non-default root canvas.
- SPEC updated: canvas is a first-class entity, the "one root canvas per project" invariant is dropped, `context_id`/`parent_id` marked transitional (dropped in 0018+).

**084 — redo SHIPPED** (the flawed row-actions layout is gone). Deployed in the same first push (bundle `_QzYXMT1`); its full e2e is **green** — this **closes the old HANDOFF item ①** (the badge/accessible-name e2e regression is resolved and live). Architecture route now shows **Name + Description only**; Add child is a hover-revealed trailing gutter, Remove lives in the selection bar.
- **084 stays PARTIAL for its OWN later scope** (unchanged): Phase 2 cross-table keyboard Tab-chain, grid unification (D3), and finding 8 (listbox selection a11y) remain open with unchecked acceptance criteria. Do **not** archive 084.

**089-D1 — SHIPPED + deploying (global rich-text toolbar).** The vision's second half landed independently of the canvas merge. Commits `feat(089-D1)` phases 1-5 + `fix(089-D1)` P6 (latest `ded2ff4`), pushed to `main`; **deploying now** (bundle was flipping off `BDPZ_8NE` as this was written — **confirm the latest hash** before assuming what's live). **Frontend-only + a value-gated heal-on-load — NO schema migration.** **`verify:fast` 1424 tests + full e2e 51/0.**
- **What landed** (terse): a global `FormatStrip` (`src/components/FormatStrip.tsx`) in the context bar, bound to the focused rich editor via a focused-editor registry (`src/store/focusedEditor.ts`), focus-reveal (no always-on chrome). **Every prose field is now rich** — `tier1_purpose.body` (Purpose now a standalone `RichTextEditor`), `tier1_props.description`, `tier2_entries.description`, and `contexts.justification` (new `richtext` `EditableGrid` cell kind with a Cmd+Enter=commit-down / Esc=revert / Tab-stays-out escape grammar). Plain↔Lexical bridge in `src/domain/richText.ts`; repaired search/status consumers of justification. The heal (`src/store/richTextConvert.ts`, wired in `DesignSurface`) converts legacy plain strings to Lexical JSON on project open, bypasses the command log, is idempotent, and **re-heals cells an un-upgraded peer clobbers back** (LWW is value-blind).
- **Remaining 089 work**: **rich identifier columns** (`*.name` / `contexts.symbol`) — deferred, **owner decision pending** (they feed the duplicate-detection tuple hash and `InlineEdit` callers lack `readOnly` threading; keep plain OR a single-line-rich variant); **D3** zoom canvas — **gated behind a spike** that must also **reconcile the canvas-substrate contradiction** in the issue doc (the owner Decision picks React Flow, but the appended library research §5 recommends rejecting it for a CSS-transform plane + `@panzoom/panzoom`). (**D2 shipped this same cycle** — see next entry.)

**089-D2 — SHIPPED & LIVE (three tiers as one lane page).** The three tier routes now render as **side-by-side vertical lanes on one page** — the "one coherent instrument" half of the vision, without any pan/zoom infra. Commits `feat(089-D2)` phases 1-5 + `fix(089-D2)` (latest `b6ae24d`), pushed to `main`; **LIVE at `index-D26xjZ_8.js`, 8/8 browser-verified** (2026-07-18): three lanes side-by-side, per-lane sticky headers not jumbled, ⌘2 scroll-to-lane, Export clickable, FormatStrip binds, only benign 401s. **Frontend + store changes — NO schema migration** (positions stay derived from the existing ordinal `sort`). **`verify:fast` 1446 unit + full e2e 53/0.**
- **What landed** (terse): `src/components/WorkspaceSurface.tsx` co-mounts all three surfaces as flex-column lanes in the shared `.surface` scroll region. **Route grammar RETAINED** — `/foundation|architecture|design[/:ctx…]?view=&canvas=` still parse / serialize / drive `tab--active`; D2 only changed WHAT the route mounts and ADDED a scroll-to-lane effect (`src/shell/laneTarget.ts`), so tabs + ⌘1/2/3 still `navigate()` and now also scroll (kept 011/012/090 + most e2e intact). New **`activeLane` slice** (`src/store/activeLane.ts`) re-scopes Design's capture-phase `c` / `v` / `d` verbs to the active lane (no lane active ⇒ no-ops). **Cross-lane reactive refresh** (`notifyLocalApply` in `src/store/sync.ts`, wired into `tier2.ts` promote / rename-propagate / resolve) refreshes the co-mounted Design lane by bumping the 075B `*AppliedAt` signals — offline-safe, respects 090 canvas-scoping. **Per-lane sticky context headers** (Architecture quick-jump; Design breadcrumbs + 090 `CanvasSwitcher` + view-toggle + coverage) since the single shell `.context-bar` can't host three tiers; shell bar now hosts only the focus-revealed D1 `FormatStrip`. **Narrow-screen reflow** at ≤1024px stacks the 3 columns.
- **Two real regressions caught by the full-e2e gate + fixed** (not papered over): (1) the sticky lane-header (`z-index: 5`) painted over the body-portaled Radix `.popover`, making Export unclickable → `.popover` z-index raised to 50 (above header, below palette at 100); (2) `FormatStrip` didn't bind on a justification cell's autoFocus (StrictMode dev double-invoke unbound the editor) → fixed in the autoFocus effect (order-independent, scoped).
- **CI-only flake that blocked the first D2 deploy (fixed)**: the first D2 push (`6916024`) passed local `verify:fast` + full e2e 53/0 but **CI `verify` FAILED** on `foundation.spec:99` (existing-scenario formatted-prose reload-persist), so `deploy` was skipped. Root cause: D2 co-mounts all three surfaces → heavier reload; the existing_scenario commit-on-blur write is fire-and-forget with no completion signal, and the prior 500 ms wait raced the reload under CI load — the write was **lost** (empty editor on reload, confirmed via CI screenshot). Fixed (`b6ae24d`) by giving the local PGlite write CI-safe headroom (3 s) before reload. **Lesson (non-negotiable #2 in action): CI e2e catches races local `--workers=1` doesn't.**
- **Known follow-up (not blocking)**: undo/redo of cross-tier ops has the same co-mount staleness the reactive refresh fixed on the forward paths — worth a later pass (**issue 092**).

## Owner live-smoke — DONE this session (090 + 088 archived to done/)

**Both 090 and 088 have been live-verified and archived to `docs/issues/done/`.** The owner-run signed-in smoke passed: the 090 canvas switcher shows independent dimension/context sets (no leakage), the 084 Architecture layout is correct, and the 088 "Sync error" banner is gone (self-heals to `Synced`). The section below is retained as **reference** — the debug-API recipe and live-smoke checks that Tooling points to, reusable for the next live-verify (e.g. once 089-D2 finishes deploying).

**(a) Debug-API DB checks** (049 SELECT-only API over the ALB). Get the token, then query:
```
TOKEN=$(aws secretsmanager get-secret-value \
  --secret-id arn:aws:secretsmanager:us-east-1:975049998516:secret:DebugTokenSecret0E11C939-uC7geGa8eYXb-uCCrkF \
  --query SecretString --output text --region us-east-1 --profile phani-quadnomics)
ALB=Gede-T-Alb16-7PG12HghU4Wa-1482490479.us-east-1.elb.amazonaws.com
curl -H "x-debug-token: $TOKEN" http://$ALB/debug/db/counts
```
Confirm via `POST /debug/db/query {"sql":"…"}`:
- `SELECT count(*) FROM canvases` is non-zero and matches (one root canvas per project + one child per distinct child context).
- **Backfill is lossless**: `SELECT count(*) FROM dimensions WHERE canvas_id IS NULL` **= 0** and same for `contexts` (no stranded rows).
- The **`app_user` GRANT on `canvases` exists** (`SELECT * FROM information_schema.role_table_grants WHERE table_name='canvases' AND grantee='app_user'`) — this is the correction #1 that would have `42501`'d cloud writes.
- (Token is in Secrets Manager; the read is classifier-gated → owner-run.)

**(b) Signed-in UI smoke** on the heavy account (`jrkphani@gmail.com`, ~12+ junk projects = built-in stress). Confirm:
- The **canvas switcher works**: create a second root canvas, switch between them, delete one and **Undo** it; the two canvases show **independent** dimension/context sets (no leakage — proves the phase-4a repoint).
- The **084 Architecture layout** is correct: **Name + Description only** (Add child in the hover gutter, Remove in the selection bar).
- The **088 "Sync error" banner is gone** (self-heals silently to `Synced`) — **verified gone this session, 088 archived to `done/`**. (If it ever recurs → candidate (B), run the 078 instrumentation playbook.)
- **How**: the scratchpad live-smoke pattern — a standalone Playwright `.mjs` in `scratchpad/*/run.mjs` driving the live URL, signing in via `#hero-email`/`#hero-password` (those scripts hardcode the owner's plaintext password → gitignored, owner-run). `window.__gede` introspection is removed — verify via UI/DOM only.

**090 and 088 are archived in `done/`.** The same flow applies to the next live-verify (owner archives after live-smoke; don't pre-move) — 089-D2 is the next candidate once its bundle is confirmed live.

## Still open / next steps (priority order)

- **① Continued session 2026-07-18 shipped 092 + 087 + Design-lane relayout + 089-D3 spike/P0** (see "## Latest" at top). **Live-verify these once CI deploys** — confirm the new bundle hash, then eyeball: the Design lane shows the ring **on top** (larger) with the table below; a sustained write failure reads "Changes not saving"; undo of a cross-tier promote refreshes the Design register with no reload. 089-D2 remains LIVE + 8/8-verified (`index-D26xjZ_8.js` was the pre-session hash). **089-D3 P1 is the next build step but is GATED on owner go-ahead** (introduces React Flow into prod).
- **② 089 — unified canvas workspace** (`docs/issues/089-unified-canvas-workspace.md`): **D1 (global rich-text toolbar) SHIPPED** and **D2 (three tiers as one lane page) SHIPPED** this cycle (see What shipped this cycle). Remaining: **(a) rich identifier columns** — deferred, **owner decision pending** (tuple-hash + `InlineEdit` `readOnly` hazards); **(b) D3** pan/zoom *zoom* canvas — **gated behind a spike** that must first **reconcile the React-Flow-vs-CSS-transform substrate contradiction** (owner Decision picks React Flow; appended §5 research rejects it for a CSS-transform plane + `@panzoom/panzoom`); **(c) the D2 undo/redo cross-lane-staleness follow-up** (**issue 092**) — the reactive refresh fixed the forward promote/rename/resolve paths, but undo/redo of those cross-tier ops still leaves the co-mounted Design lane stale (later pass, not blocking). Zoom-canvas work is unblocked by 090 (canvas is a real entity; 089 renders N canvases as N clusters) but must not start until that substrate call is made.
- **③ 087 — surface silent write failures**: OPEN, not started (the write-side complement of 086).
- **⑤ 091 — "The item you tried to change no longer exists" surfaces during editing** (`docs/issues/091-editing-item-no-longer-exists-note.md`): OPEN, observed live on the D1 build (owner screenshot), not root-caused. Write-API `unknown_entity` rejection (`handler.ts:99` via `checkTenancy`) — a client `update`/`delete` targeting a row the server has no record of; likely a stale-delete echo OR the D1 heal-on-load enqueuing an `'update'` for a not-yet-synced row. Cosmetic (self-heals to `Synced`) but a scary banner. **Next: CloudWatch the `…WriteApiFunction…` log for the exact rejected mutation (table/op/rowId/reason); watch whether it recurs on the D2 build.**
- **④ 0018+ cleanup migration**: drop `dimensions.context_id` / `contexts.parent_id` and repoint the server dimension-floor query (`countLiveDimensions`, `src/server/writeApi/store.ts:442-449`) off `context_id` onto `canvas_id` (090 Open Question 5). The read path is already on `canvas_id`; these columns are transitional.
- **076-class backend 502s** on `/sync/v1/shape`: distinct ShapeProxy/Electric hiccup seen during prior verify reloads; own look (CloudWatch the ShapeProxy/SyncContainer).
- **RLS no-op in prod** (`gede_admin` owner; 080's `/accept` is the sole authz boundary), **CDK-synth `/tmp` leak**, and **test-data clutter** on the heavy account — all still standing.
- **Housekeeping — stale worktrees (~3.1 GB)** under `.claude/worktrees/agent-*` from prior crashes: **4 clean** (safe to `git worktree remove`), **2 with 1 unmerged commit each** (inspect before removing), **1 dirty** (has uncommitted changes — do not remove blind). Check each for unpushed work first.

## Non-negotiables (how to work)
1. **TDD, red first.** 2. **Deploy = push to `main`** — CI is the only path, and **CI `verify` includes Playwright e2e that local `verify:fast` does NOT run**, so a UI change can pass locally and fail CI (exactly what happened with the 084 badge). Run the affected e2e (or the full suite) locally before pushing a UI change. 3. **Schema only via migrations** (latest **`0017`**; next **`0018`**; adding one bumps `deploy/cdk/test/migration-stack.test.ts:85` count 18→19 + the `.snap`; Electric-synced tables need `REPLICA IDENTITY FULL` + a `GRANT … TO app_user` + a `syncScope`/`WORKSPACE_SCOPE_SQL` entry + the ~13-registry envelope fan-out — see 090's plan + its `## What shipped`; a new parent FK needs `DEFERRED_FK_COLUMN`/apply-order consideration à la 088/090). 4. **Verify subagent claims against the code** (adversarial review caught the 088 reentrancy gap; the 090 build caught real plan errors — the GRANT, the FORMAT_VERSION bump, the read-path repoint). 5. **Verify LIVE, don't infer** — the whole 088 saga is a fix passing tests + adversarial review yet failing at production scale; and the live build hash was mid-flip as this was written. 6. **Screenshot UI changes before shipping** — the FIRST 084 shipped a layout bug (verbs in a data column) that only a screenshot caught; the redo was screenshot-verified. 7. **Orchestration**: ≤3 subagents concurrent; system memory <75% (`memory_pressure` "free %"); reap stray playwright/chromium; e2e `--workers=1`.

## Tooling
- **AWS profile matters.** App account **`975049998516` = profile `phani-quadnomics`**; the owner's default shell profile is a different personal account, so raw `aws …` fails cross-account — use **`AWS_PROFILE=phani-quadnomics`**. The AWS MCP (`mcp__aws-api__call_aws`) is authenticated to the app account.
- **049 debug API** (SELECT-only over the ALB): token + recipe in the live-smoke section above. **ALB DNS**: `Gede-T-Alb16-7PG12HghU4Wa-1482490479.us-east-1.elb.amazonaws.com`. Secret ARN: `arn:aws:secretsmanager:us-east-1:975049998516:secret:DebugTokenSecret0E11C939-uC7geGa8eYXb-uCCrkF`.
- **CloudWatch** = fastest server debugger: Write Lambda `…WriteApiFunction…`, ShapeProxy `…ShapeProxyFunction…`, Electric `…SyncContainerLogGroup…-iC9rDOPocc3x` (logs `Received relation "public"."<table>"` when a change flows through WAL), migration-runner logs `applied N (…); skipped M` (how 0017 was confirmed live).
- **Live-smoke pattern**: standalone Playwright `.mjs` in `scratchpad/*/run.mjs` drive the live URL. **Working reference (2026-07-18): `scratchpad/live-verify-089-d2/run.mjs`** — takes creds from **env vars** (`GEDE_EMAIL`/`GEDE_PASSWORD`) so no secret is embedded. **Two selectors drifted from older scripts** (fix in any stale ones): sign-in is now behind a **"Sign in" button → `/login`** (then `#hero-email`/`#hero-password`), not an inline hero form on the root; and projects render as **`div.project-row[role="button"]`** (not `<a>` anchors) that re-sort while syncing, so open one via **focus + Enter**, not `.click()`. **Security note**: older scratchpad scripts hardcode the owner's plaintext password — **gitignored** (`scratchpad/` excluded), plaintext on local disk only; prefer the env-var pattern. Accounts: A=`jrkphani@gmail.com` (heavy/stress), B=`jrkphani@icloud.com`. `window.__gede` introspection is REMOVED — verify via UI/DOM only. **Local app screenshots**: `npx playwright test <spec>` auto-starts the vite dev server (`playwright.config.ts` webServer, port 5173); a scratch spec that builds data + `locator.screenshot()` is how the 084 redo was visually verified.
- **Classifier** blocks mutating AWS + `git push` + secret-plaintext reads from the agent — hand the user a `!` command for those. `gh run watch` is flaky on this network; a `gh run view --json status` poll loop is more resilient.

## Docs map
`docs/SPEC.md` (canvas now first-class — §2 glossary, §3 data model, §4.6) · `TECH_STACK.md` · `STYLE_GUIDE.md` · `SITEMAP.md` (carries a forward-pointer to 089/090's route changes) · `adr/` · `DEPLOYMENT.md §9` · `docs/issues/` (README = index; `done/` = shipped; **090 + 088 SHIPPED & archived to done/**, **084/087 open**, **089 — D1 + D2 SHIPPED; D3 + rich identifiers OPEN**). Knowledge graph in `graphify-out/` (gitignored).

import { messages as enUS } from './en-US.js';
import type { Messages } from './types.js';

/** English (United Kingdom): no spelling in the mail copy differs from en-US. */
export const messages: Messages = { ...enUS };

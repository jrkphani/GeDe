/**
 * Row hierarchy (HIER-01..10): the outline as pure projections and the
 * validated writes over it. Depth and collapse live per row in `rowMeta`.
 */
export * from './outline.js';
export * from './mutations.js';
